{
  "enabledPlugins": {
    "chrome-devtools-mcp@claude-plugins-official": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r 'if (.tool_input.subagent_type // \"\") | test(\"codex-rescue\") then \"{\\\"hookSpecificOutput\\\": {\\\"hookEventName\\\": \\\"PreToolUse\\\", \\\"permissionDecision\\\": \\\"deny\\\", \\\"permissionDecisionReason\\\": \\\"Blocked by repo contract: codex-rescue via the Agent tool is background-only, silently drops --wait/--fresh, and writes to the shared checkout after the handoff appears to have returned. Delegate with: codex exec --sandbox workspace-write --json. See CLAUDE.md.\\\"}}\" else \"{}\" end'"
          }
        ]
      }
    ]
  }
}
